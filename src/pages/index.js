import * as React from "react";
import Layout from "../components/layout";

const HomePage = () => {
  return (
    <Layout pageTitle="Home" pageHeading="Home">
      <h2>Test assignment</h2>
      <p>
        Use React (preferably with TypeScript) to create a responsive webpage.
        Your page should include some basic layout components (e.g. header,
        footer, menu, logo-placeholder…).
      </p>
    </Layout>
  );
};

export default HomePage;
